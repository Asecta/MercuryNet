package com.pandoaspen.mercury.common.service.redirect.model;

public enum RedirectStatus {
    PLAYER_NOT_FOUND,
    SERVER_NOT_FOUND,
    SUCCESS,
    ALREADY_CONNECTED,
    SERVER_DISCONNECTED,
    CONNECTION_CANCELLED,
    CONNECTION_IN_PROGRESS,
    UNKNOWN,
    DISCONNECT_WHITELIST;
}
